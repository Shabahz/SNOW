# iOS (SwiftUI)

This folder is reserved for the SwiftUI client app.

Suggested next steps:
1. Create an Xcode project named `HouseholdBudget`.
2. Add API client services targeting `http://localhost:8000` during simulator runs.
3. Implement auth, household setup, budgets, transaction list, and Plaid Link handoff.
